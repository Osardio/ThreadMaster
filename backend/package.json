{
  "name": "threadmaster_backend",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "author": "osardio",
  "scripts": {
    "generate": "npx prisma generate && npx prisma migrate dev",
    "dev": "npm run generate && ts-node index.ts",
    "serve": "npx prisma generate && npx prisma migrate deploy && ts-node index.ts"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.7",
    "prisma": "^4.10.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "postgres": "^3.3.3",
    "ts-node": "^10.9.1"
  }
}
