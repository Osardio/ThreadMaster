{
  "name": "threadmaster_backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "author": "osardio",
  "scripts": {
    "build": "npm install && npx prisma migrate dev && tsc",
    "dev": "ts-node index.ts",
    "serve": "npm ci && npx prisma migrate deploy && tsc && node dist/index.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.7",
    "prisma": "^4.10.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "postgres": "^3.3.3"
  }
}
